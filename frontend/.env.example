# Prefer 127.0.0.1 over localhost on macOS — localhost often resolves to ::1 (IPv6),
# while uvicorn binds to 127.0.0.1 (IPv4). Docker may also own *:8000 on IPv6.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
